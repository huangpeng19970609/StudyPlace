## 前言： 内容很多，范围很广，请坚持。

### 一 其他

```js
1 typeof null 的结果是什么？
2 var msg; typeof msg; 这两处的打印结果是什么？
3 undefined 与 null 的关系
4 有哪些情况的条件表达式为 false ？
5 如果判断一个对象是空的对象？
6 0.1 + 0.2 等于什么？
7 isNaN('10') isNaN(true) 的结果是什么?
8 转为整数的方法有哪些? 比如 "1000" "10blue" "23.3333333.100.3000"
9 改变字符串是否代表着销毁原先的字符串？ null undefined 有 toString方法吗？ 他们会返回什么?
10 定义了一个 a = {},其如何追溯道原型的?
11 如何循环一个对象呢?  三种方法,讲述特别点
12 如何终止一个循环? (比如for; for item in arr; )
   请讲述 forEach some every filter map 这些循环的return的特别情况。
13 switch语句中的case支持表达式吗？
14 一个函数 返回了return; 此时打印此函数返回结果是什么? 函数声明时候若是不写参数声明可以调用吗？
15 若同时存在两个同名的函数会怎么样？
16 请示范一个模板占位符！并用类似$符号再实现一个
17 当我们使用 大于号 小于号的时候 是基于变量的什么来比较?
```

---

### 二 变量 作用域 内存

```js
1  变量可以划分哪两大类，其又可以怎么区分?
2 为什么基本类型可以调用方法呢? 其过程是什么?
3 基本类型与引用类型的真正区别是什么？
4 这道题告诉我们什么事情?
  function hello(obj) {
    obj.name = '123';
    obj = new Object();
    obj.name = 123456;
  }
  hello(obj);
5 typeof 的使用 
  一个字符串如何可以骗过 typeof 呢？同理其它
  instanceof 判断什么？依据是什么?
6 到底如何才能正确的获取 一个对象的类? 强烈推荐使用 的这种方法！
7 看这道代码题,请再控制台打印出如下
  function add(x, y) { return x + y}
  add函数中 哪个属性代表了作用域链?
  这个数组类型的 属性 是按照什么顺序的?
  有一个Closure类型的是什么意思?
8 明白了作用域链的意义以后， 我们可以做出什么样的优化?
9 请解释以下，作用域的以下概念
  执行环境可以细分为 【全局环境】 与 【局部环境】，如何去理解【执行环境】与【作用域链】？若函数为函数的时候， 【活动对象】优先是其【执行环境】的哪个参数?
10 此代码的结果
    var (i =0; i<10; i++) {
   }
   console.log(i);
   为什么会出现这种情况? 
   答: 使用【var】来声明的变量会被__________
   使用let 声明的变量，其局部作用域是相对于什么？
11 
  var a = 10;
  a.toFixed(2);  返回结果是什么?
12
  返回第一个单独的字符, 有两个办法 =>
  str1 = str1.concat(str2); 也可以拼接字符串吗?
13
  截取字符串 的 子字符串的方法有三种. => slice substring subStr
  subStr的第二个参数代表什么含义?
14
  字符串的查找函数是什么?其方法第二个参数代表什么含义?
15
  如何删除 字符串的 前置与后置的空格符?
16
  如何将字符串大小写转换?
17 
  如何将字符串 变为数组？请说两种方法.

18 
  单体内置对象 Global对象，不属于任何其他对象的方法与函数就是Global对象的属性。
19 函数名 与 函数本有什么关系呢？

20 按值传递
   书上说: ECMAScript中所有函数的参数都是_____的.
   也就是说，把函数外部的值复制给函数内部的参数，就和把值从一个变量复制到另一个变量一样

   基本数据类型 =>【引用传递】；对象 =>【引用传递】
   其实，JS函数参数 应该共享传递的.看似 引用传递（实则共享传递）
   共享传递是指: 在传递对象的时候，[传递对象的引用的副本]。
      var obj = {
        value: 1,
      };
      function foo(o) {
        o = 2;
        console.log(o); //2
      }
      foo(obj);
      console.log(obj.value) // 1

  形参上的属性指向的内存和实参上的属性的内存是共享的.
  其实就是 复制了 引用的地址而已，所以都是按传递，你细想下就知道为什么不可以成功了，这是 对象的 堆的问题！
  共享传递: 只是特地起的名字的而已， 还是叫做引用传递就可。不过要避免对于形参的对象地址的更改!因为共享了地址

21 for (let i in xxx) 
   for (let i of xxx) 的区别在于什么?

22 合并对象的两种方式
23 判断数组的三个办法
24 toString() 与 valueOf() 的区别是什么? 一个数组的toString方法
25 push pop shift unshift调用以后会返回什么?
26 请用sort函数，将数组从小到大排序
27 您会用slice切割数组吗?
28 请使用splice实现 增 删 改, 请讲述对此三个参数的理解
29 indexOf() 是一种什么样的查询？ 模糊还是精确？
30 arr的迭代方法是什么意思?有什么共性?5个噢
31 两个Date实例进行比较，内部调用了什么?比较的是什么?
32 Date 转换 与 RegExp 略. 范围很多，日后工作需要请回顾。

```

---

### 函数

```js

  1 【函数表达式】 与 【声明式函数】最大的区别是什么?
  2  一个函数的名字 其本质是什么?所以函数也可以作为____来参.
  3 arguments，说下你的理解. 是数组吗?可以显示创建吗？
  4 

```
### ES6语法

```js
   1 解构赋值要 要以【】为解构的牢记准则？
   2 声明式函数 对象中（对象中存在函数） 箭头函数中 三者的 this 指向区别， 还有 一个 普通函数的this是基于什么的？
   3 箭头函数可以 通过方法改变this指向吗?
   4 有哪些方法可以改变this指向？他们的区别在于什么？
   5 箭头函数的 this是再什么时候开始绑定的？ 定义函数时？还是执行过程中？
```























```

```